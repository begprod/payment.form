export  default  class Validation {
	constructor(form, textField) {
		this.form = document.querySelector(form);
		this.textField = this.form.querySelector(textField);
		this.init();
	}

	init() {

	}
}